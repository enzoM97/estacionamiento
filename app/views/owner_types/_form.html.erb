<%= form_for(@owner_type) do |f| %>
  <% if @owner_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@owner_type.errors.count, "error") %> prohibited this owner_type from being saved:</h2>

      <ul>
      <% @owner_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
